<%= form_with(model: @question, local: true) do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: "What are you curious about?" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>